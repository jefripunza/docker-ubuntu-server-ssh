version: '3.8'

services:
  ubuntu-ssh:
    build: .
    image: jefriherditriyanto/vps-ubuntu-server:1.0.5
    # https://gvisor.dev/docs/user_guide/install/
    runtime: runsc
    privileged: true
    dns:
      - 8.8.8.8
      - 1.1.1.1
    # ports:
    #   - "2222:22"
    #   - "6080:6080"
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
    storage_opt:
      size: '60G'
